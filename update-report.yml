name: '📊 Actualizar Reporte de Sentimiento'

on:
  schedule:
    - cron: '0 11 * * *'  # 11:00 UTC = 08:00 Argentina
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'main.py'
      - 'report_generator.py'
      - '.github/workflows/update-report.yml'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  scrape-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 📥 Checkout repositorio
        uses: actions/checkout@v4

      - name: 🐍 Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 📦 Instalar dependencias
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          python -m textblob.download_corpora lite || true

      - name: 🔍 Ejecutar scraping y generar reporte
        env:
          CI: 'true'
          OUTPUT_DIR: 'docs'
          # ── Método 1: Cookies (principal) ──
          TWITTER_COOKIES: ${{ secrets.TWITTER_COOKIES }}
          # ── Método 2: Múltiples cuentas (fallback + rotación) ──
          TWITTER_ACCOUNTS: ${{ secrets.TWITTER_ACCOUNTS }}
          # ── Método 3: Cuenta simple (fallback básico) ──
          TWITTER_USERNAME: ${{ secrets.TWITTER_USERNAME }}
          TWITTER_EMAIL: ${{ secrets.TWITTER_EMAIL }}
          TWITTER_PASSWORD: ${{ secrets.TWITTER_PASSWORD }}
        run: |
          python main.py

      - name: 📤 Commit y push del reporte
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/ tweets_data.json
          if git diff --staged --quiet; then
            echo "Sin cambios en el reporte."
          else
            TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M UTC")
            git commit -m "📊 Reporte actualizado: ${TIMESTAMP}"
            git push
          fi
